# ============================================================
# Factory AI — PPE Helmet/Mask Detection Dataset
# ============================================================
# YOLOv8 dataset YAML for helmet & mask detection training.
#
# Classes:
#   0: helmet       — worker wearing a safety helmet
#   1: no_helmet    — worker without a safety helmet
#   2: mask         — worker wearing a face mask
#   3: no_mask      — worker without a face mask
#
# Generated by: training/download_dataset.py
# ============================================================

# Paths (absolute — updated by download_dataset.py)
path: ./datasets/ppe_helmet_mask
train: train/images
val: valid/images
test: test/images

# Number of classes
nc: 4

# Class names
names:
  0: helmet
  1: no_helmet
  2: mask
  3: no_mask
